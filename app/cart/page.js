import CartContent from './CartContent';

export const metadata = {
    title: 'Your Cart | GISMAT MANDI',
    description: 'Review your cart and proceed to checkout.',
};

export default function Page() {
    return <CartContent />;
}
